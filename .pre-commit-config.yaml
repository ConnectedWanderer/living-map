repos:
  - repo: local
    hooks:
      - id: format-check
        name: Format check (Prettier + Ruff)
        entry: bash scripts/format.sh --check
        language: system
        stages: [pre-commit]
        pass_filenames: false
